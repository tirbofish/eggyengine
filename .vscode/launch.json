{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "${workspaceFolder}/zig-out/bin/eggyengine-demo",
            "cwd": "${workspaceFolder}",
            "valuesFormatting": "parseText"
        }
    ]
}